import { styled, Stack } from '@mui/material';

/**
 * Styled root container for the ActionButtonsGroup
 */
export const StyledRoot = styled(Stack)(() => ({
  // Base styles for the Stack component
  // Additional styling can be added here if needed in the future
}));
