import { styled } from '@mui/material/styles';
import { Box } from '@mui/material';

export const StyledChangePasswordForm = styled(Box)(() => ({
  display: 'flex',
  flexDirection: 'column',
})) as typeof Box;
