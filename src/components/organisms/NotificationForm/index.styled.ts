import { styled } from '@mui/material/styles';
import { Box } from '@mui/material';

export const StyledNotificationForm = styled(Box)(() => ({
  display: 'flex',
  flexDirection: 'column',
})) as typeof Box;
