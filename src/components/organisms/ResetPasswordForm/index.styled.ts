import { styled } from '@mui/material/styles';
import { Box } from '@mui/material';

export const StyledResetPasswordForm = styled(Box)(() => ({
  width: '100%',
  maxWidth: '400px',
  margin: '0 auto',
}));
