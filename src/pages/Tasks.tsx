import Layout from '@containers/Layout';
import TasksContainer from '@containers/Tasks';

const Tasks = () => {
  return (
    <Layout>
      <TasksContainer />
    </Layout>
  );
};

export default Tasks;
