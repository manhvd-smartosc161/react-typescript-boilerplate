import Layout from '@containers/Layout';
import HomeContainer from '@containers/Home';

const Home = () => {
  return (
    <Layout>
      <HomeContainer />
    </Layout>
  );
};

export default Home;
