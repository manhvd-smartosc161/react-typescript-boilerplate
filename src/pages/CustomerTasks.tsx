import Layout from '@containers/Layout';
import CustomerTasksContainer from '@containers/CustomerTasks';

const CustomerTasks = () => {
  return (
    <Layout>
      <CustomerTasksContainer />
    </Layout>
  );
};

export default CustomerTasks;
